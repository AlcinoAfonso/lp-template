export default function WaButton({ href }) {
  return (
    <a href={href} className="text-base font-semibold leading-[1.4] tracking-tight font-inter px-8 py-4 rounded-[16px] bg-brand text-white inline-block">
      WhatsApp: Saiba como é feito na prática
    </a>
  );
}
